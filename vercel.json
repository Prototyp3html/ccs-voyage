{
  "framework": "vite",
  "buildCommand": "npm run build && npm run vercel:html",
  "outputDirectory": "dist/client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
